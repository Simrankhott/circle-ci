version: 2.1

jobs:
  myjob:
    docker: 
    - image: cimg/base:stable
    steps:
    - checkout
    - run:
        name: hello-world
        command: echo "my first circle-ci job"

workflows:
  myjob-workflow:
    jobs:
     - myjob

