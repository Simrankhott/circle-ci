version: 2.1
orbs:
  docker: circleci/docker@1.7.0

jobs:
  myjob:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: hello-world
          command: echo "my first circle-ci job"
  jobs:
  secondjob:
    docker:
      - image: cimg/base:stable
    steps:
      - docker/build:
          image: myimage 
          tag: latest 

workflows:
  myjob-workflow:
    jobs:
      - myjob
      - secondjob
